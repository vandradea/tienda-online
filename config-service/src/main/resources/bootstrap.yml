server:
  port: 8081

sprint:
  cloud:
    config:
      server:
        git:
          uri: hhttps://github.com/vandradea/tienda-online.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
          default-label: "main"
  security:
    user:
      name: root
      password: s3cr3t
